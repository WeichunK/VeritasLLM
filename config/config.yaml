defaults:
  - model: transformer
  - data: default
  - training: default
  - experiment: default
  - _self_

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}

project_name: VeritasLLM
seed: 42
debug: false
