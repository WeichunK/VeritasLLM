batch_size: 32
gradient_accumulation_steps: 1
max_steps: 10000
learning_rate: 3e-4
weight_decay: 0.1
warmup_steps: 100
precision: bf16 # options: fp32, fp16, bf16
grad_clip: 1.0
save_interval: 1000
eval_interval: 500
log_interval: 10
resume_from_checkpoint: null
device: cuda
compile: true
